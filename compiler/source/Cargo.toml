[package]
name = "factc-source"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FactTest front end: semantic-island scanner, tokenizer and statement parser for ASCII Systems Diagrams (ASCII-GRAMMAR.md). Geometry-independent by construction."

[lib]
path = "src/lib.rs"

[dependencies]
factc-foundation = { path = "../foundation" }
