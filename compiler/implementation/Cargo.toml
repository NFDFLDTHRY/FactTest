[package]
name = "factc-implementation"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FactTest implementation contracts: registry dialect (ImplementationContract, RepresentationContract, ConversionContract, CodegenRecipe, metric evidence, machine epochs) and H_G / H_A(E) enumeration (IMPLEMENTATION-CONTRACTS.md, REPRESENTATION-TRANSFER.md, LOWERING-MODEL.md sections 4-6)."

[lib]
path = "src/lib.rs"

[dependencies]
factc-foundation = { path = "../foundation" }
factc-source = { path = "../source" }
factc-semantic = { path = "../semantic" }
factc-capability = { path = "../capability" }
