[package]
name = "factc-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FactTest no_std compiler kernel: phase facade and bootstrap ABI (C14)."

[lib]
path = "src/lib.rs"

[dependencies]
factc-foundation = { path = "../foundation" }
