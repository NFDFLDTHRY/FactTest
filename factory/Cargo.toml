[package]
name = "factory"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "FactTest Factory plane: StructuralDelta router, isolated workpieces, stations, receipts, independent verification, integration gate, re-inspection."

[lib]
path = "src/lib.rs"

[[bin]]
name = "factory"
path = "src/main.rs"

[dependencies]
factc-foundation = { path = "../compiler/foundation" }
