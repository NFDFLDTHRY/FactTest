{
  "fixture_id": "F3-evidence",
  "station_id": "S-EVIDENCE",
  "selected_inputs": [
    "the D27 graph",
    "evidence/D28/identity",
    "the runbook"
  ],
  "narrowed_may_change": [
    "evidence/D28/selection.json",
    "evidence/D28/logs/"
  ],
  "job_parameters": {
    "commands": [
      {
        "program": "sh",
        "args": [
          "-c",
          "node tests/reprove/select.mjs --graph design/environment-map/graph.json --identity evidence/D28/identity --dimensions tests/reprove/dimensions.json --runbook tests/reprove/runbook.json --obligations tests/reprove/obligations.json --pass D28 --out evidence/D28/selection.json && node -e 'const s=JSON.parse(require(\"fs\").readFileSync(\"evidence/D28/selection.json\"));console.log(\"evaluated\",s.evaluated,\"selected\",s.selected,\"gaps\",s.gaps.length,\"unmapped\",s.unmapped_obligations.length);if(s.gaps.length||s.unmapped_obligations.length)process.exit(1)'"
        ],
        "log": "evidence/D28/logs/F3-select.log",
        "cwd": "."
      }
    ]
  },
  "expected_outputs": [
    "evidence/D28/selection.json"
  ],
  "narrowed_may_read": [
    "*"
  ],
  "evidence_requirements": [
    "receipt"
  ],
  "delta_id": "D28-DECISIONS-AND-CONSTRAINTS"
}
