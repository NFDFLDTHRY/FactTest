# D25 - Intended Physical Webapp / Runtime Execution

STATUS: INTENDED ASCII (assembled before routing; source of record for delta D25-PHYSICAL-RUNTIME, workpiece W31,
base 76f4441)
REQUEST: design/materialization/D21-D26-WHOLE-REPO-EXECUTION-PROMPT.md, D25 PHYSICAL WEBAPP / RUNTIME EXECUTION: "Do
the bundles generated by D24 actually behave correctly in physical browser environments?  Use freshly generated
artifacts, not old evidence bundles.  Record exact environment identity ... Exercise CURRENT implemented runtime
behaviors ... Run multi-specimen bundles from D24.  PUBLIC HTTPS: ... If it cannot: DO NOT substitute localhost as
proof of public HTTPS.  Produce: exact deployable probe, expected evidence schema, artifact hashes, exact user-run
instructions, and preserve [UNK] until external evidence exists."
ISSUES: I-05 (B: the selfhost probe defaults to a historical bundle), I-11 (B: the shell's controls never exercised),
I-33 (D: runtime self-integrity - decide).  Found by this task: none in the runtime (no repair needed).
SCOPE: execute and record; a shell-driving probe with the exact environment identity; no compiler, template or kernel
change; the public-HTTPS claim stays [UNK] with an exact boundary and a deployable probe.

## 0. Observation before drawing

```text
runtime today  the D24 bundles: runtime.js transfer(relation, bytes) by verified requirement, the tape bound to the
               strategy data identity; the shell (index.html) offers init, a hex input + transfer button per authored
               relation, a release button per guarded backend; the D24 harnesses drive window.factRuntime directly -
               the shell's own controls were never clicked (I-11)
selfhost       tests/selfhost/primitives-probe.mjs defaulted BUNDLE_DIR to evidence/D7/physical/compile/bundle (I-05);
               D24 ran it with BUNDLE_DIR = the fresh bundle (P01, P04 RUN) but the default remained
scratch trial  host/harness/webapp-probe.mjs (new) drove the fresh Byte Relay bundle through its buttons: E0 WEBGPU
(76f4441)      A,B exact, release -> E1 CPU_WASM64 A,B exact, stale plan invalidated, SW registered, CacheStorage
               populated, no runtime codegen, only bundle files requested, bundle unchanged; without WebGPU CPU_WASM64
               at E0; with COOP/COEP from the origin server crossOriginIsolated true + SharedArrayBuffer present, from
               the bundle alone false/absent; the same for ledger-mirror, pixel-vault, dual-stream through
               tests/physical/run-webapp.mjs; the selfhost probes on the fresh bundle: P01-P06, P08, P09, P14-P16 RUN,
               P10 OBS (fresh kernel in a worker), P07 GAP (persist() false in the headless shell, as since D12)
environment    Chromium HeadlessChrome/141.0.7390.37 (@9f043f63..., V8 14.1.146.11, /opt/pw-browsers/chromium-1194/
               chrome-linux/chrome), --enable-unsafe-webgpu --enable-features=Vulkan --use-angle=swiftshader
               --enable-unsafe-swiftshader --ignore-gpu-blocklist; WebGPU adapter vendor google, architecture
               swiftshader, isFallbackAdapter true (no hardware GPU); Linux 6.18.44, x64, 4 logical CPUs
public HTTPS   raw.githack.com, rawcdn.githack.com, nfdfldthry.github.io: CONNECT refused (403) by the egress policy;
               raw.githubusercontent.com: 200 but text/plain + x-content-type-options: nosniff + CSP "default-src
               'none'; sandbox" -> module scripts refused; the bundle.json served there for the pushed D24 bundle
               names the same bundle_id as the fresh build (0f4a1ec1...).  No reachable public origin can host the
               module WebApp: [UNK] with an exact boundary; localhost is not substituted.
self-integrity I-33: CODEGEN-BUNDLE-CONTRACT.md does not require the runtime to verify its own files; claiming it
               would change the contract -> kept unclaimed (owner), FACT-D24-RUNTIME-SELF-INTEGRITY stays [GAP]
judged by      the D22 Factory (76f4441)
```

## 1. The shell-driven probe (host/harness/webapp-probe.mjs; generic)

```text
input          --bundle DIR (served on 127.0.0.1, --isolate adds COOP/COEP) or --url URL (a deployed copy);
               --payloads; --no-webgpu; --loss B|none; --expect-e0/--expect-e1 from the caller (the specimen)
identity       Browser.getVersion (product, revision, jsVersion), chromium.executablePath(), launch options, os +
               uname, origin, isSecureContext, crossOriginIsolated, typeof SharedArrayBuffer, navigator.gpu adapter
               (vendor, architecture, device, description, isFallbackAdapter, features, limits), HTTP status and
               response headers of index.html, every artifact fetched and compared with bundle.json, the proof-set
               pins, the runtime's machine epochs
drive          click "init (E0)"; per relation x payload: fill "hex bytes for <relation>", click "transfer <relation>",
               read the shell's snapshot (#out); click "release <backend>" for the loss backend; transfer again at E1;
               read service-worker registrations and CacheStorage keys
verdict        every EXECUTE exact (harness sha256(input) == runtime output_sha256), loss witnessed (admission
               rejected, E1, one ObservationDelta), stale plan invalidated, artifacts as recorded, bundle unchanged,
               only bundle files requested, no runtime codegen, SW registered, CacheStorage populated, expectations
output         probe-record.json (identity, steps, verdict), runtime-evidence.json, evidence-tape.ascii
```

## 2. The physical runner and the public boundary (tests/physical/)

```text
run-webapp.mjs        per specimen: fresh build (certificate PASS), check-bundle, webapp-probe x3 (webgpu with the
                      specimen's loss/E1, no-webgpu, isolated), observe bound; selfhost primitives with BUNDLE_DIR =
                      the fresh Byte Relay bundle and KERNEL_WASM = the fresh kernel; public-https-probe; the map of
                      every runtime behaviour of the prompt to its evidence (summary.json behaviours)
public-https-probe.sh the candidate routes (githack dev/CDN, GitHub Pages, raw.githubusercontent html + module):
                      CONNECT refusal / status / content-type / nosniff / CSP -> hosts_module_webapp per route; the
                      served bundle.json at the pushed path vs the fresh build; status REACHABLE / UNREACHABLE
PUBLIC-HTTPS-PROBE.md what to deploy (content types, no blocking CSP), the exact user-run command (webapp-probe
                      --url), the expected evidence schema, how the [UNK] closes (an external run's evidence through
                      the Factory)
primitives-probe.mjs  BUNDLE_DIR required (exit 2 without a generated bundle); the summary records the bundle hashes
```

## 3. Runtime behaviours of the prompt -> evidence

```text
wasm64 execution / known-answer / admission / selection    no-webgpu + webgpu probe records (admission receipts with
                                                            DISCOVER/REQUEST/PROBE, activation, EXECUTE exact)
bundle verification                                         bundle-certificate.json per fresh build; check-bundle
WebGPU request/admission, adapter classification            identity.page.adapter (swiftshader, fallback) + receipts
controlled device loss, epoch transition, stale-plan        controlled-loss step: admission REJECTED, epochs E0,E1,
invalidation, fallback/reselection                          ObservationDelta {stale_plans, replacement_plan}, E1 transfers
no runtime codegen, bundle unchanged across reselection     verdict flags (requests, console, hashes before/after)
Service Worker, CacheStorage                                service-worker step (registrations, cache keys)
offline launch, OPFS, IndexedDB, restart survival, Worker   selfhost P01, P04/P03, P06, P03/P01, P10 on the fresh bundle
isolation headers where implemented                         isolated run (COOP/COEP from the server): true; alone: false
public HTTPS                                                public-https.json: UNREACHABLE (exact boundary), [UNK]
```

## 4. Evidence binding (tests/envmap/facts/D25.json; epoch D25; no reconciliation)

```text
IMPL-WEBAPP-PROBE, IMPL-PHYSICAL-RUNNER; PROBE-PHYSICAL-WEBAPP, PROBE-PUBLIC-HTTPS; ENV-D25-HOST
FACT-D25-RUNTIME-PHYSICAL          [RUN]  four fresh bundles through their shells, three configurations, every
                                          behaviour with fresh evidence and identity (I-11 closed)
FACT-D25-SELFHOST-PRIMITIVES-FRESH [RUN]  P01-P16 on the fresh bundle (I-05 closed); P07 GAP, P10 OBS as observed
FACT-D25-ISOLATION-ORIGIN          [OBS]  isolation is the origin server's, not the bundle's
FACT-D25-PUBLIC-HTTPS              [UNK]  the exact boundary; deployable probe; served bundle.json identified
I-33                               kept unclaimed (owner); FACT-D24-RUNTIME-SELF-INTEGRITY stays [GAP]
```

## 5. Mutation plan by station (delta D25-PHYSICAL-RUNTIME, workpiece W31, base 76f4441)

```text
F0-doc       S-DOC       this ASCII; LEDGER (D24 AFTER, D25 BEFORE); delta + fixtures
F1-fixture   S-FIXTURE   tests/physical/{run-webapp.mjs, public-https-probe.sh, PUBLIC-HTTPS-PROBE.md},
                         primitives-probe.mjs (BUNDLE_DIR required: exit 2 without a bundle, no historical default),
                         components.json, issues.json, facts/D25.json: syntax, JSON, register checks
F2-web       S-WEB       host/harness/webapp-probe.mjs: syntax, generic scan, identity / shell-driving / --url present
F3-browser   S-BROWSER   identity -> evidence/D25/identity/
F4-build     S-BUILD     cargo build factc; the wasm64 release kernel (identity == the D24 pin: nothing changed);
                         run-webapp -> evidence/D25/physical (4 specimens x 3 configurations, selfhost, public
                         boundary); checks on every verdict, identity and selfhost verdict
F5-doc       S-DOC       design/execution-manifest rebuilt at 76f4441 (+ additions); deterministic
F6-evidence  S-EVIDENCE  evidence/D25/{manifest, issues, gate}.json
F7-doc       S-DOC       epochs/D25.json (build-fact-epoch); graph; views; SCHEMA row; ENVIRONMENT-MAP row; HANDOFF;
                         README
F8-evidence  S-EVIDENCE  validate, Q01-Q22, stale, merge-check, render-check, Q22 check, D18/D18R/D20-SYNC/D23/D24
                         gates, capability + implementation gates, status scan, handoff check, audit -> retire ->
                         audit (W29; the stopped W30 stays KEEP), cleanup gate, index
F9-doc       S-DOC       observed record
reinspect    STRUCTURAL CHECK / GATE greps; node --check of the probe; validate; merge-check; render-check;
             epochs/D25.json rebuilt from the graph merged through D24 and compared
```

## 6. Predictions (from the dry run on a scratch clone at 76f4441)

```text
P1  fresh builds: 4 specimens, certificate PASS, integrity PASS; the kernel built by F4 has the D24 identity
    ef5d886a... (no compiler change)
P2  shell-driven runs: 4 specimens x 3 configurations PASS with the specimens' expectations; every transfer exact;
    loss witnessed and the stale plan invalidated wherever a loss is attempted (all four with WebGPU); no runtime
    codegen, only bundle files requested, bundle unchanged, artifacts as recorded, SW registered, CacheStorage
    populated; crossOriginIsolated false from the bundle alone, true (+ SharedArrayBuffer) with server headers
P3  identity: HeadlessChrome/141.0.7390.37 (@9f043f63..., V8 14.1.146.11), /opt/pw-browsers/chromium-1194/chrome-linux/
    chrome, adapter google/swiftshader isFallbackAdapter true (19 features, 35 limits recorded); host Linux
    6.18.44 x64, 4 CPUs; 4 toolchains, 45 source tips (5 moved)
P4  selfhost on the fresh bundle: P01-P06, P08, P09, P14-P16 RUN; P10 OBS; P07 GAP; bundle hashes recorded;
    public HTTPS: UNREACHABLE (3 CONNECT refusals, 2 text/plain + nosniff + sandbox CSP); the served bundle.json of
    the pushed D24 copy equals the fresh build
P5  manifest at 76f4441 + additions: 4147 files (PRODUCTION 68, FACTORY 24, TEST 106, FIXTURE 111, REFERENCE 86,
    RECORD 905, LAW 32, HISTORICAL 11, EVIDENCE 2804) in 69 components, unassigned 0, byte-identical rebuild; gate
    PASS with 35 issues (A 11, B 10, C 2, D 3, E 1, F 4, G 4)
P6  epoch D25: 20 nodes / 36 edges (ENV 1, IMPL 2, PROBE 2, EV 11, FACT 4); merged 1352 / 3483; validate 42;
    Q22 191 current facts (RUN 120, OBS 49, GAP 12, ERR 5, UNK 5); RUN 120 = 119 claimable + 1 invalidated; open
    stops GAP 10, ERR 5, UNK 5 (FACT-D25-PUBLIC-HTTPS added); the two D25 [RUN] facts newest evidence D25;
    stale 387
P7  hygiene: status scan PASS, handoff check PASS, D18/D18R/D20-SYNC/D23/D24 gates PASS; W29 RETIRABLE -> REMOVED,
    W29-stage KEEP; index 64 files
```

## 7. Invariants

```text
I1  no compiler, template or kernel change: the kernel identity built by F4 equals the D24 pin ef5d886a...
I2  fresh artifacts only: every bundle probed is generated by F4; the selfhost probe refuses to run without one
I3  the public-HTTPS claim is [UNK] with an exact boundary; localhost is never substituted for it
I4  the shell is driven through its own controls; expectations come from the specimens, never from the harness
I5  law and pass documents, factory/, fixtures/, earlier epochs, evidence, receipts and records untouched
```

## 8. Structural check

```text
[x] fresh artifacts: every bundle and the kernel are built by this delta's build station, never read from evidence
[x] every environment identity dimension of the prompt is recorded per probe (browser product, revision, JS engine,
    executable, launch flags, OS/host, origin, secure context, crossOriginIsolated, SharedArrayBuffer, adapter
    identity, isFallbackAdapter, features/limits, artifact hashes, toolchain, machine epoch)
[x] every CURRENT runtime behaviour of the prompt maps to fresh physical evidence (section 3) or an exact boundary
[x] the D24 multi-specimen bundles run, through their own shells, in three browser configurations
[x] public HTTPS: not reachable -> deployable probe, evidence schema, artifact hashes, user-run instructions; [UNK]
    preserved; localhost not substituted
[x] I-05 and I-11 repaired; I-33 decided (owner, unclaimed); no unrelated capability family expanded
```

STRUCTURAL CHECK: PASS
