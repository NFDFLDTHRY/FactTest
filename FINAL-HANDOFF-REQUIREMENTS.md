# Final Fable 5.1 Handoff Requirements

D13 ANNOTATION: the title names the historical Fable 5.1 session (historical).  The requirements below remain live law for any agent or person continuing FactTest; the live handoff is docs/HANDOFF.md, which carries them forward model-independently.

STATUS: PASS 6 OUTPUT

This is not yet the final condensed ASCII system prompt.
It defines what that prompt must preserve.

## Manufacturing law

```text
ASCII -> Factory -> isolated Workpiece -> Verify -> Integration Gate -> Repo
```

Never Agent -> canonical repo as working surface.

## Preserve project identity

- no_std/no-third-party-crate Rust compiler kernel
- ASCII Systems Diagram is source language and human/AI interaction layer
- capability-parametric Chrome WebApp target
- wasm64 direction; no silent wasm32 fallback
- Factory != compiler
- backend != station
- planner != verifier
- source semantics != runtime observation
- FactTest.git != generated WebApp

## Authority

Re-check current living authorities at implementation time.
Use exact clause links for externally constrained claims.

## Language

Implement semantic islands, unique elaboration, geometry non-authority, typed IDs, nominal types, transfer modes, effects/capabilities, refinement, invariants, tests/evidence, governance, objectives, canonical rendering and ANALYZE vs BUILD.

Never fill semantic gaps with AI inference.

## Compiler machinery

Generic:
- scanner/parser
- resolver
- TypedSystemIR
- obligation extraction
- capability lowering
- representations/conversions
- implementation hypergraph
- CandidateStrategy planner
- independent verifier
- VerifiedStrategy
- CodegenRecipe engine
- codegen
- BundleVerifier
- runtime admission/selector
- evidence/observed-ASCII renderer

## Adaptive law

```text
CandidateStrategy
 -> VerifiedStrategy
 -> codegen all verified variants
 -> ActivationReceipt @ epoch
 -> ActivePlan
```

Runtime cannot invent a non-emitted implementation.

## Commissioning-first gate

Before broad capability expansion, materialize and physically prove Byte Relay and all P6 tests.

No commissioning-specific compiler shortcuts.

## Evidence honesty

Never equate:
- synthetic model with physical execution
- manifest with installation
- hardware preference with hardware execution
- API presence with admission
- planner choice with verification
- codegen output with runtime success

On mismatch/failure/stale authority/moved base/missing evidence/ambiguous semantics:
return to ASCII with ERR/GAP/UNK.
