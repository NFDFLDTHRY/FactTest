{
 "schema": "facttest-fact-epoch-spec/1",
 "summary": "physical webapp / runtime execution: the four fresh D24 bundles driven through their own shells in Chromium (with WebGPU on the SwiftShader fallback adapter, without WebGPU, cross-origin isolated by server headers) with the exact environment identity; every runtime behaviour of the D25 prompt with fresh evidence; the selfhost primitives on the fresh bundle; the public-HTTPS route an exact [UNK] boundary with a deployable probe",
 "implementations": [
  {
   "id": "IMPL-WEBAPP-PROBE",
   "repo_path": "host/harness/webapp-probe.mjs",
   "kind": "harness",
   "note": "the generated shell driven through its own controls in Chromium, the environment identity recorded beside every observation; loopback-served (optionally COOP/COEP) or a public https:// URL (D25)"
  },
  {
   "id": "IMPL-PHYSICAL-RUNNER",
   "repo_path": "tests/physical/{run-webapp.mjs,public-https-probe.sh}",
   "kind": "harness",
   "note": "every specimen freshly built and driven in three browser configurations, observed; the selfhost primitives on the fresh bundle; the public-HTTPS boundary probe (D25)"
  }
 ],
 "probes": [
  {
   "id": "PROBE-PHYSICAL-WEBAPP",
   "command_or_operation": "node tests/physical/run-webapp.mjs --factc BIN --out DIR --kernel WASM --pushed-bundle PATH: for every fixtures/genericity specimen factc check build (certificate PASS), tests/genericity/check-bundle.mjs, then host/harness/webapp-probe.mjs three times (WebGPU with the specimen's loss/E1 expectations; --no-webgpu; --isolate with COOP/COEP): click init, fill the hex input and click transfer per authored relation and payload, click release for the loss backend, transfer again at E1; identity from Browser.getVersion, chromium.executablePath, launch options, os, location, isSecureContext, crossOriginIsolated, SharedArrayBuffer, navigator.gpu.requestAdapter (info, isFallbackAdapter, features, limits), every fetched file's sha256 vs bundle.json, the proof-set pins; factc observe of the shell's tape with the bundle manifest; then tests/selfhost/primitives-probe.mjs with BUNDLE_DIR = the fresh Byte Relay bundle and KERNEL_WASM = the fresh kernel",
   "expected_observations": [
    "every specimen: certificate PASS, three shell-driven runs PASS with the specimen's expectations, observe bound",
    "crossOriginIsolated false from the bundle alone and true with server headers",
    "selfhost P01, P04 RUN on the fresh bundle"
   ],
   "failure_meaning": [
    "a fresh bundle does not behave in the physical browser as the runtime contract claims, or the shell's own controls do not drive it"
   ],
   "implemented_by": "IMPL-WEBAPP-PROBE"
  },
  {
   "id": "PROBE-PUBLIC-HTTPS",
   "command_or_operation": "sh tests/physical/public-https-probe.sh OUT BUNDLE PUSHED: curl -D - of the candidate public routes (githack dev and CDN hosts, the GitHub Pages origin, raw.githubusercontent for index.html and a module script) recording CONNECT refusals, status, content-type, x-content-type-options and content-security-policy; hosts_module_webapp per route; the served bundle.json at the pushed path compared with the fresh build's bundle_id; the deployable probe for an external run is tests/physical/PUBLIC-HTTPS-PROBE.md (node host/harness/webapp-probe.mjs --url https://... with the specimen's expectations)",
   "expected_observations": [
    "REACHABLE with a usable origin, then an external webapp-probe run recorded separately; or UNREACHABLE with the exact refusal or header per route and the [UNK] preserved"
   ],
   "failure_meaning": [
    "never a runtime failure: the record describes the execution environment's public-HTTPS boundary"
   ],
   "implemented_by": "IMPL-PHYSICAL-RUNNER"
  }
 ],
 "facts": [
  {
   "id": "FACT-D25-RUNTIME-PHYSICAL",
   "subject": "the four bundles generated by the D24 compiler (Byte Relay, ledger-mirror, pixel-vault, dual-stream), driven as webapps in Chromium 141 headless shell through their own shells",
   "predicate": "with WebGPU (SwiftShader: vendor google, architecture swiftshader, isFallbackAdapter true; features and limits recorded) and without it, every authored transfer of every payload driven through the shell's hex input and transfer button emerges exact (harness sha256 of the input equals the runtime's output sha256) under the plan the specimen expects; the release button for the guarded backend rejects its admission, opens E1 with one ObservationDelta naming the stale plan, and the specimen's expected reselection follows (Byte Relay WEBGPU -> CPU_WASM64, dual-stream CPU_WASM64 -> WEBGPU, ledger-mirror and pixel-vault -> no plan, nothing invented); pixel-vault without WebGPU activates no plan; only the bundle's own files are requested, no code is generated at runtime, the bundle files on disk are unchanged across reselection and every fetched file matches bundle.json; the shell registers the service worker and CacheStorage holds the shell; the tape is bound to the bundle (factc observe evidence_lineage OBS, source_of_record OBS)",
   "status": "RUN",
   "note": "I-11 closed: the shell's own controls are exercised; the environment identity (browser product/revision/V8, executable, launch flags, host, origin, secure context, isolation, adapter, artifact hashes, toolchain, epochs) is in every probe-record.json; hardware GPU absent (fallback adapter only) - a hardware-adapter run is a different environment, not claimed",
   "source_ref": "evidence/D25/physical/summary.json",
   "required_environment": [
    "Chromium headless shell 141 (Playwright 1.56.1) with --enable-unsafe-webgpu --enable-features=Vulkan --use-angle=swiftshader --enable-unsafe-swiftshader --ignore-gpu-blocklist, and without them",
    "loopback origin http://127.0.0.1 (a secure context)",
    "HOST_NATIVE_SET factc built at the D25 tree"
   ],
   "constraint_refs": [],
   "probe": "PROBE-PHYSICAL-WEBAPP",
   "evidence": [
    {
     "path": "evidence/D25/physical/summary.json",
     "observed_result": "4 specimens x 7 checks PASS; behaviours -> evidence map"
    },
    {
     "path": "evidence/D25/physical/bundles/byte-relay/webgpu/probe-record.json",
     "observed_result": "E0 WEBGPU plan 1 A,B exact; release WEBGPU -> E1 CPU_WASM64 plan 0 A,B exact; stale plan 1 invalidated; identity recorded"
    },
    {
     "path": "evidence/D25/physical/bundles/dual-stream/webgpu/probe-record.json",
     "observed_result": "E0 CPU_WASM64 plan 0 feed + drain exact; release CPU_WASM64 -> E1 WEBGPU plan 3 feed + drain exact"
    },
    {
     "path": "evidence/D25/physical/bundles/pixel-vault/no-webgpu/probe-record.json",
     "observed_result": "no WebGPU: WEBGPU rejected at discover, no active plan, every stash NO_ACTIVE_PLAN"
    },
    {
     "path": "evidence/D25/physical/bundles/ledger-mirror/webgpu/runtime-evidence.json",
     "observed_result": "admission receipts (DISCOVER/REQUEST/PROBE known answer), activation, EXECUTE records, the E0 -> E1 ObservationDelta"
    }
   ],
   "implemented_by": [
    "IMPL-WEBAPP-PROBE",
    "IMPL-PHYSICAL-RUNNER",
    "IMPL-FACTC"
   ],
   "stale_if": [
    {
     "dimension": "browser.version",
     "relation": "a different Chromium, WebGPU implementation or adapter"
    },
    {
     "dimension": "repo.commit",
     "relation": "any change of compiler/codegen/templates, host/harness/webapp-probe.mjs or fixtures/genericity/"
    }
   ]
  },
  {
   "id": "FACT-D25-SELFHOST-PRIMITIVES-FRESH",
   "subject": "the self-hosting primitive probes P01-P16 against the freshly generated Byte Relay bundle and the freshly built wasm64 kernel",
   "predicate": "P01 offline launch from the bundle's versioned CacheStorage before and after a browser restart, P02 SW cannot self-update, P03 storage survives restart per origin, P04 the bundle served entirely from OPFS by a service-worker router offline and after restart, P05 SW cannot grant isolation, P06 IndexedDB atomic pointer / compare-and-swap, P08 candidate confinement, P14 broken-SW recovery, P15 interrupted SW update, P16 confined generation load: RUN; P10 the kernel in a dedicated worker (ANALYZE 1, BUILD 1): OBS; P07 persistent storage: GAP (persist() false in the headless, non-installed shell); P09 git objects in the browser: RUN; the probe takes the bundle from BUNDLE_DIR (required) and records its file hashes",
   "status": "RUN",
   "note": "I-05 closed: no historical bundle is an input; the D7 evidence bundle stays history",
   "source_ref": "evidence/D25/physical/selfhost/summary.json",
   "required_environment": [
    "Chromium headless shell 141 (Playwright persistent contexts), loopback origins 47311/47312"
   ],
   "constraint_refs": [],
   "probe": "PROBE-PHYSICAL-WEBAPP",
   "evidence": [
    {
     "path": "evidence/D25/physical/selfhost/summary.json",
     "observed_result": "bundle sha256 per file; 13 probes with their verdicts"
    },
    {
     "path": "evidence/D25/physical/selfhost/probes/P01-GENERATED-BUNDLE-OFFLINE.json",
     "observed_result": "RUN: transfer exact offline before and after restart"
    },
    {
     "path": "evidence/D25/physical/selfhost/probes/P04-OPFS-SERVED-APP.json",
     "observed_result": "RUN: served by opfs-sw, transfer exact, after restart exact"
    },
    {
     "path": "evidence/D25/physical/selfhost/probes/P10-KERNEL-IN-WORKER.json",
     "observed_result": "OBS: the fresh wasm64 kernel in a dedicated worker"
    }
   ],
   "implemented_by": [
    "IMPL-SELFHOST-PROBES",
    "IMPL-PHYSICAL-RUNNER"
   ],
   "stale_if": [
    {
     "dimension": "browser.version",
     "relation": "a different Chromium"
    },
    {
     "dimension": "repo.commit",
     "relation": "any change of compiler/codegen/templates or tests/selfhost/"
    }
   ]
  },
  {
   "id": "FACT-D25-ISOLATION-ORIGIN",
   "subject": "cross-origin isolation of the generated webapp",
   "predicate": "loaded from the bundle alone the page is not cross-origin isolated and SharedArrayBuffer is absent (the generated bundle emits no COOP/COEP; a service worker cannot grant them, P05); with Cross-Origin-Opener-Policy: same-origin and Cross-Origin-Embedder-Policy: require-corp from the origin server the same bundle is cross-origin isolated with SharedArrayBuffer present and every transfer still exact - isolation is a property the deploying origin supplies, not the bundle",
   "status": "OBS",
   "note": "observed on all four specimens; threads/shared memory are not claimed by any bundle (no runtime path uses SharedArrayBuffer)",
   "source_ref": "evidence/D25/physical/bundles/byte-relay/isolated/probe-record.json",
   "required_environment": [
    "loopback origin with and without COOP/COEP headers"
   ],
   "constraint_refs": [],
   "probe": "PROBE-PHYSICAL-WEBAPP",
   "evidence": [
    {
     "path": "evidence/D25/physical/bundles/byte-relay/isolated/probe-record.json",
     "observed_result": "cross_origin_isolated true, shared_array_buffer true, transfers exact, loss and reselection as without isolation"
    },
    {
     "path": "evidence/D25/physical/bundles/byte-relay/webgpu/probe-record.json",
     "observed_result": "cross_origin_isolated false, shared_array_buffer absent"
    }
   ],
   "implemented_by": [
    "IMPL-WEBAPP-PROBE"
   ],
   "stale_if": [
    {
     "dimension": "browser.version",
     "relation": "a different Chromium"
    }
   ]
  },
  {
   "id": "FACT-D25-PUBLIC-HTTPS",
   "subject": "execution of a generated bundle from a public HTTPS origin",
   "predicate": "not observed: from this execution environment the githack dev and CDN hosts and the GitHub Pages origin are refused at CONNECT by the egress policy, and raw.githubusercontent.com (reachable) serves every file as text/plain with x-content-type-options: nosniff and a sandboxing Content-Security-Policy, which refuses module scripts - no reachable public origin can host the module WebApp; the bundle.json served there for the pushed copy names the same bundle_id as the fresh build, so the deployable artifact is publicly identified; the deployable probe (host/harness/webapp-probe.mjs --url https://... with the specimen's expectations), its expected evidence schema and the user-run instructions are in tests/physical/PUBLIC-HTTPS-PROBE.md",
   "status": "UNK",
   "note": "explicit stop: an environmental boundary (class E), not a runtime failure; localhost is not substituted; the claim moves to [RUN] only with an external run's evidence committed through the Factory",
   "source_ref": "evidence/D25/physical/public-https/public-https.json",
   "required_environment": [
    "a public HTTPS static origin serving the bundle with correct content types and without a blocking CSP"
   ],
   "constraint_refs": [],
   "probe": "PROBE-PUBLIC-HTTPS",
   "evidence": [
    {
     "path": "evidence/D25/physical/public-https/public-https.json",
     "observed_result": "UNREACHABLE: 3 CONNECT refusals, 2 text/plain + nosniff + sandbox CSP; served bundle.json equals the fresh build"
    }
   ],
   "implemented_by": [
    "IMPL-PHYSICAL-RUNNER",
    "IMPL-WEBAPP-PROBE"
   ],
   "stale_if": [
    {
     "dimension": "network_egress.policy",
     "relation": "a reachable public origin able to host module scripts"
    }
   ]
  }
 ]
}
